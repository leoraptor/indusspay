export interface cardType {
  id: number;
  img: string;
  bgColor: string;
  title: string;
  sub: string;
}

export interface whyUsType {
  id: number;
  img: string;
  title: string;
  sub: string;
}
